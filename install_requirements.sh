#!/bin/bash
#apt-get update && apt-get upgrade

# install neovim
#add-apt-repository ppa:neovim-ppa/stable && apt-get update && apt-get install neovim

# install nvm
# curl -o- htts://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bashp

#install node
# nvm install stable
